{
  "name": "fridge2recipe",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:web": "pnpm --filter @fridge2recipe/web dev",
    "dev:api": "pnpm --filter @fridge2recipe/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "echo \"No tests yet\" && exit 0"
  }
}
